/**
 * @deprecated
 * OnboardingSidebar has been superseded by the shared OnboardingStepper system.
 * Use `shared/components/onboarding-stepper/OnboardingStepper` instead.
 *
 * Kept as a thin re-export so any legacy import doesn't break during cleanup.
 */
export { DesktopStepper as default } from "@/shared/components/onboarding-stepper";
