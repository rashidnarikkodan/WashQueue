/** Re-exports from the shared Stepper. Import from "@/shared/components/stepper" instead. */
export type { StepDef as Step, StepWithStatus, StepStatus, StepperRenderProps } from "@/shared/components/stepper";
